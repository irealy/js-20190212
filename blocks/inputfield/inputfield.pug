div.inputfield.pure-control-group
    label(for=name)
    input.inputfield__input(id=name, type=type, name=name, value=value, placeholder=label)
    if required
        span.pure-form-message-inline Обязательное поле
